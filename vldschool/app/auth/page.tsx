import { redirect } from "next/navigation";
import React from "react";

export default function AuthScreen(authMode: string) {
    return (
        <div>
            Connexion à la plateforme
        </div>
    ) 
}
